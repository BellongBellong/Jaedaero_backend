# Shared non-secret settings only.
app.name=jaedaero
app.environment=local

# CODEF OAuth endpoint. Credentials belong only in application-local.properties.
codef.oauth.token-url=https://oauth.codef.io/oauth/token
codef.oauth.timeout-seconds=30
codef.api.base-url=https://development.codef.io

# KRX Open API key belongs only in application-local.properties.
krx.api.base-url=https://data-dbg.krx.co.kr
