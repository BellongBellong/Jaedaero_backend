<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jaedaero.domain.goal.mapper.GoalMapper">
    <select id="findTargetAmount" parameterType="long" resultType="long">
        SELECT g.target_amount FROM goal g JOIN users u ON u.user_id = g.user_id
        WHERE g.user_id = #{userId} AND u.is_withdrawn = FALSE
    </select>
    <update id="updateTargetAmount" parameterType="map">
        UPDATE goal g JOIN users u ON u.user_id = g.user_id
        SET g.target_amount = #{targetAmount}
        WHERE g.user_id = #{userId} AND u.is_withdrawn = FALSE
    </update>
</mapper>
