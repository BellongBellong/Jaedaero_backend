<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jaedaero.domain.auth.mapper.InvestmentPreferenceMapper">
    <select id="countActiveUserByUserId" parameterType="long" resultType="int">
        SELECT COUNT(1)
        FROM users u
        WHERE u.user_id = #{userId}
        AND u.is_withdrawn = FALSE
    </select>

    <insert id="upsertInitialPreference" parameterType="map">
        INSERT INTO investment_badge (
            user_id,
            initial_preference
        )
        VALUES (
            #{userId},
            #{investmentPreference}
        )
        ON DUPLICATE KEY UPDATE
            initial_preference = VALUES(initial_preference)
    </insert>

    <insert id="upsertGoalTargetAmount" parameterType="map">
        INSERT INTO goal (
            user_id,
            target_amount
        )
        VALUES (
            #{userId},
            #{targetAmount}
        )
        ON DUPLICATE KEY UPDATE
            target_amount = VALUES(target_amount)
    </insert>
</mapper>
