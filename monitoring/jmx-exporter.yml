# 성능 테스트에서는 JVM, Tomcat, HikariCP MBean 전체를 노출한다.
# 운영 적용 전에는 실제 사용하는 MBean만 남겨 메트릭 cardinality를 제한한다.
startDelaySeconds: 0
ssl: false
lowercaseOutputName: true
lowercaseOutputLabelNames: true
rules:
  - pattern: '.*'
